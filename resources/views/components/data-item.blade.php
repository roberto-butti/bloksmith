   <div class="flex justify-between border-b pb-2 pt-2 hover:bg-neutral-100">
        <span class="text-gray-500 font-medium">{{ $label }}</span>
        <span class="text-gray-900 font-semibold">{{ $value }}</span>
    </div>
